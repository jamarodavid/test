Edit File Konfigurasi Apache:

nano /etc/httpd/conf/httpd.conf

Tambahkan Konfigurasi Referrer-Policy:

<IfModule mod_headers.c>
    Header always set Referrer-Policy "same-origin"
</IfModule>

simpan dan restart web server
